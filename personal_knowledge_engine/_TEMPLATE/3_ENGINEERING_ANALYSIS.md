# 工程与代码实现分析 (Engineering Analysis)

## 代码依赖与环境分析 (Dependencies & Environment Analysis)

### 核心依赖分析
**[质询]** 列出`requirements.txt`中最核心的Top 3性能相关依赖项。

| 依赖库 | 版本要求 | 作用说明 | 性能影响 |
|:---|:---|:---|:---|
| [库名1] | [版本] | [作用描述] | [性能影响分析] |
| [库名2] | [版本] | [作用描述] | [性能影响分析] |
| [库名3] | [版本] | [作用描述] | [性能影响分析] |

### 自定义算子分析
**[质询]** 代码中是否包含任何自定义C++/CUDA算子（`.cpp`/`.cu`文件）？其作用是什么？

- **是否包含自定义算子**: [是/否]
- **算子文件路径**: [文件路径列表]
- **算子功能说明**: [每个算子的具体作用]
- **性能优化目的**: [为什么需要自定义算子]

## 核心技术解耦与映射 (Core Technology Mapping)

### 理论-代码映射表
**[质询]** 针对论文的每一个核心创新点，完成下表：

| 理论创新点 | 代码实现证据 (文件路径+函数/类) | 静态性能预判 | 依赖的非标库/算子 |
|:---|:---|:---|:---|
| [创新点1] | [文件路径:行号] [函数/类名] | [访存/计算密集, 潜在瓶颈] | [特殊依赖] |
| [创新点2] | [文件路径:行号] [函数/类名] | [访存/计算密集, 潜在瓶颈] | [特殊依赖] |
| [创新点3] | [文件路径:行号] [函数/类名] | [访存/计算密集, 潜在瓶颈] | [特殊依赖] |

### 关键代码片段分析
#### 核心实现1: [功能名称]
**文件路径**: `[路径/文件名.py:行号]`
```python
# [在此粘贴关键代码片段，并添加注释分析]
```
**性能分析**: [分析这段代码的计算/访存特性]

#### 核心实现2: [功能名称]
**文件路径**: `[路径/文件名.py:行号]`
```python
# [在此粘贴关键代码片段，并添加注释分析]
```
**性能分析**: [分析这段代码的计算/访存特性]

## 推理流程静态分析 (Inference Flow Analysis)

### 主要推理路径
**[质询]** 用伪代码或流程图，描述主模型`forward`函数中输入张量的完整变换路径。

```python
# 主要推理流程伪代码
def forward(x):  # x.shape = [B, H, W, C]
    # Step 1: [操作描述]
    x = operation1(x)  # x.shape = [新维度]
    
    # Step 2: [操作描述]
    x = operation2(x)  # x.shape = [新维度]
    
    # Step 3: [操作描述]
    x = operation3(x)  # x.shape = [新维度]
    
    # ...
    return output  # output.shape = [最终维度]
```

### 张量形状变化分析
**[质询]** 在该路径上，张量的`shape`发生了几次关键变化？在哪些模块变化的？

| 步骤 | 模块名称 | 输入Shape | 输出Shape | 变化类型 | 变化原因 |
|:---|:---|:---|:---|:---|:---|
| 1 | [模块1] | [B, H, W, C] | [B, H', W', C'] | [降采样/升采样/重塑] | [原因说明] |
| 2 | [模块2] | [B, H', W', C'] | [B, H'', W'', C''] | [变化类型] | [原因说明] |
| ... | ... | ... | ... | ... | ... |

## 资源消耗理论分析 (Resource Consumption Analysis)

### 模型规模分析
**[质询]** 模型的总参数量是多少？（来源：论文/README/代码估算）

- **总参数量**: [具体数值] (来源: [论文表格/README/代码计算])
- **可训练参数**: [具体数值]
- **固定参数**: [具体数值]

#### 参数分布
| 模块名称 | 参数量 | 占比 | 参数类型 |
|:---|:---|:---|:---|
| [模块1] | [数值] | [百分比] | [权重/偏置/嵌入等] |
| [模块2] | [数值] | [百分比] | [权重/偏置/嵌入等] |
| ... | ... | ... | ... |

### 内存消耗分析
**[质询]** 理论上峰值显存占用可能发生在哪个环节？请说明理由。

#### 内存占用估算
- **模型参数内存**: [数值] MB/GB
- **激活值内存**: [数值] MB/GB (峰值)
- **梯度内存**: [数值] MB/GB (训练时)
- **优化器状态**: [数值] MB/GB (训练时)

#### 峰值内存分析
- **峰值发生位置**: [具体模块/操作]
- **峰值原因**: [详细分析为什么在此处达到峰值]
- **优化建议**: [如何减少内存占用]

## 性能瓶颈预判 (Performance Bottleneck Prediction)

### 计算瓶颈
- **计算密集操作**: [列出主要的计算密集操作]
- **预期瓶颈**: [分析哪些操作可能成为瓶颈]
- **硬件需求**: [对GPU/CPU的特殊要求]

### 访存瓶颈
- **内存访问模式**: [分析主要的内存访问模式]
- **缓存友好性**: [评估算法的缓存友好程度]
- **带宽需求**: [分析内存带宽需求]

### 并行化潜力
- **可并行部分**: [分析哪些部分可以并行化]
- **并行化限制**: [分析并行化的限制因素]
- **扩展性评估**: [评估多GPU扩展性]
